@{
  Layout = "_Layout";
}
@using UniversityRegistrar.Models;

<h2> Student List </h2>

@if(Model.Count == 0)
{
  <h3>No Students Added</h3>
}

@foreach (Student item in Model)
{
  <p>@Html.ActionLink($"{item.Name}", "Details", new {id = item.StudentId}), @item.Major.DepartmentName</p>
}